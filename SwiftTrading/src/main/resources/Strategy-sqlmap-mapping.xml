<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
      "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.citi.swifttrading.domain.Strategy">

	<cache />

	<resultMap type="Strategy" id="resultListStrategy">
		<result column="ID" property="id" />
		<result column="Name" property="strategyName" />
		<result column="Description" property="description" />
		<result column="ExitPoint" property="exit" />
		<result column="SecurityName" property="security.nameAbbreviation" />
	</resultMap>

	<insert id="insert-strategy" parameterType="Strategy" useGeneratedKeys="true"
		keyProperty="id">
		insert into
		strategy(Name,Description,ExitPoint,SecurityName)
		values(#{strategyName},#{description},#{exit},#{security.nameAbbreviation})
	</insert>

	<select id="query_All_Strategy" resultMap="resultListStrategy">
		select * from strategy
	</select>

	<select id="queryByStrategyID" resultType="Strategy" parameterType="int">
		select * from strategy where ID = #{id}
	</select>

	<update id="update-strategy" parameterType="Strategy">
		update strategy set ID=#{id},Name=#{strategyName},
		Description=#{description},ExitPoint=#{exit},SecurityName=#{security.nameAbbreviation}
		where ID=#{id}
	</update>

	<delete id="delete-strategy" parameterType="int">
		delete from strategy where ID = #{id}
		</delete>
</mapper> 